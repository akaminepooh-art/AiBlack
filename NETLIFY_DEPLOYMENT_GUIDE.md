# AI BLACK ScopeZX - Netlify Deployment Guide

## 🚀 Netlify展開の概要

このアプリケーションは**フロントエンド（静的サイト）**と**バックエンド（Node.js API）**で構成されています。

---

## ⚠️ 重要な注意点

### **Netlifyでホストできるもの**
- ✅ **フロントエンド**: `public/` フォルダの静的ファイル（HTML, CSS, JS）
- ✅ **認証ページ**: `auth.html`（パスワード: `akirakira`）

### **Netlifyでホストできないもの**
- ❌ **バックエンドAPI**: `src/` フォルダのNode.js Express サーバー
- ❌ **Vision API呼び出し**: OpenAI APIキーが必要

---

## 📋 推奨構成

### **Option 1: フロントエンド + 外部バックエンド（推奨）**

```
┌─────────────────────────┐
│ Netlify                 │
│ - フロントエンド         │
│ - 認証ページ             │
└─────────┬───────────────┘
          │
          │ API リクエスト
          ↓
┌─────────────────────────┐
│ Heroku / Render / VPS   │
│ - Node.js API           │
│ - OpenAI Integration    │
└─────────────────────────┘
```

**実装手順**:
1. **Netlifyにフロントエンド展開**
   ```bash
   cd /path/to/trading-platform
   # publicフォルダをNetlifyにアップロード
   ```

2. **バックエンドを別サーバーに展開**
   - Heroku: https://heroku.com
   - Render: https://render.com
   - Railway: https://railway.app
   - VPS: DigitalOcean, AWS EC2など

3. **app.jsのAPI_BASEを変更**
   ```javascript
   // public/app.js
   const API_BASE = 'https://your-backend.herokuapp.com/api';
   ```

---

### **Option 2: Netlify Functions（制約あり）**

Netlify Functionsを使用してバックエンドをホストできますが、以下の制約があります:

**制約**:
- ⚠️ 実行時間: 10秒（無料）/ 26秒（Pro）
- ⚠️ Vision API: 応答時間が長い（20-30秒）→ タイムアウトの可能性
- ⚠️ コールドスタート: 初回リクエストが遅い

**実装例**:
```javascript
// netlify/functions/vision-analyze.js
exports.handler = async (event) => {
  // Vision API呼び出し
};
```

---

## 🔐 環境変数の設定

### **Netlify側（フロントエンド）**
環境変数は不要（静的ファイルのみ）

### **バックエンド側（Heroku/Render等）**
以下の環境変数を設定:
```
OPENAI_API_KEY=sk-...
PORT=3001
```

---

## 📦 Netlify展開手順

### **1. Netlifyアカウント作成**
https://www.netlify.com/

### **2. サイトをデプロイ**

#### **方法A: Netlify CLI**
```bash
npm install -g netlify-cli
cd /path/to/trading-platform
netlify deploy --dir=public --prod
```

#### **方法B: Git連携**
1. GitHubにリポジトリ作成
2. Netlifyで「New site from Git」
3. Build settings:
   - **Base directory**: `/`
   - **Publish directory**: `public`
   - **Build command**: (空欄)

#### **方法C: ドラッグ&ドロップ**
1. Netlifyダッシュボード
2. `public/`フォルダをドラッグ&ドロップ

---

## ✅ 展開後の確認

### **フロントエンド（Netlify）**
1. https://your-site.netlify.app にアクセス
2. 認証ページが表示される
3. パスワード `akirakira` を入力
4. メインアプリにリダイレクト

### **バックエンド接続確認**
1. ブラウザのコンソールを開く
2. API接続エラーが表示される（バックエンド未展開の場合）
3. バックエンドを展開後、`app.js`の`API_BASE`を更新

---

## 🐛 トラブルシューティング

### **Q: 認証ページが表示されない**
A: `netlify.toml`の設定を確認
```toml
[[redirects]]
  from = "/"
  to = "/auth.html"
  status = 200
```

### **Q: API接続エラー**
A: バックエンドのURLを確認
```javascript
// public/app.js
const API_BASE = 'https://your-backend.herokuapp.com/api';
```

### **Q: Vision API呼び出しがタイムアウト**
A: Netlify Functionsは使用せず、専用のバックエンドサーバーを使用してください

---

## 📊 コスト概算

### **Netlify（フロントエンド）**
- 無料プラン: 100GB帯域、月100GBまで
- Pro: $19/月

### **バックエンド（Heroku例）**
- Eco: $5/月
- Basic: $7/月

### **OpenAI API**
- Vision API: ~$0.01/リクエスト
- GPT-4o: ~$0.005/1Kトークン

---

## 🔗 参考リンク

- Netlify Docs: https://docs.netlify.com/
- Netlify Functions: https://docs.netlify.com/functions/overview/
- Heroku Deployment: https://devcenter.heroku.com/articles/deploying-nodejs
- Render Deployment: https://render.com/docs/deploy-node-express-app

---

## 📞 サポート

問題が発生した場合は、以下を確認してください:
1. Netlifyのビルドログ
2. ブラウザのコンソールエラー
3. バックエンドのログ（Heroku/Render等）
